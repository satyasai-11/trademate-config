server:
  port: 8001

spring:
  application:
    name: distributor-service   
  datasource:
    url: jdbc:mysql://localhost:3306/trademate?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: satyasai11
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
